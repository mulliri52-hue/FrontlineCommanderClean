name: Crea APK Android

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    name: Crea APK Android
    runs-on: ubuntu-latest

    steps:
      - name: Scarica il repository
        uses: actions/checkout@v4

      - name: Installa Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Installa Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Trova FRONTLINE_READY.zip
        shell: bash
        run: |
          set -e

          ZIP_FILE="$(find "$GITHUB_WORKSPACE" -maxdepth 2 -type f -name 'FRONTLINE_READY.zip' | head -n 1)"

          if [ -z "$ZIP_FILE" ]; then
            echo "ERRORE: FRONTLINE_READY.zip non trovato."
            exit 1
          fi

          echo "ZIP trovato: $ZIP_FILE"
          echo "ZIP_FILE=$ZIP_FILE" >> "$GITHUB_ENV"

      - name: Estrai il progetto Android
        shell: bash
        run: |
          set -e

          rm -rf extracted
          mkdir -p extracted

          unzip -q "$ZIP_FILE" -d extracted

          echo "Contenuto estratto:"
          find extracted -maxdepth 3 -type f | head -100

      - name: Trova il progetto Gradle
        shell: bash
        run: |
          set -e

          GRADLEW="$(find "$GITHUB_WORKSPACE/extracted" -type f -name gradlew | head -n 1)"

          if [ -z "$GRADLEW" ]; then
            echo "ERRORE: gradlew non trovato nel progetto estratto."
            echo "Struttura trovata:"
            find "$GITHUB_WORKSPACE/extracted" -maxdepth 5 -type f | sort | head -300
            exit 1
          fi

          PROJECT_DIR="$(dirname "$GRADLEW")"

          echo "Gradle Wrapper trovato:"
          echo "$GRADLEW"

          echo "PROJECT_DIR=$PROJECT_DIR" >> "$GITHUB_ENV"

      - name: Controlla Gradle Wrapper
        shell: bash
        run: |
          set -e

          cd "$PROJECT_DIR"

          chmod +x ./gradlew

          echo "Versione Gradle:"
          ./gradlew --version

      - name: Controlla struttura Android
        shell: bash
        run: |
          set -e

          cd "$PROJECT_DIR"

          if [ ! -f "settings.gradle" ] && [ ! -f "settings.gradle.kts" ]; then
            echo "ERRORE: settings.gradle/settings.gradle.kts non trovato."
            exit 1
          fi

          if [ ! -d "app" ]; then
            echo "ERRORE: cartella app non trovata."
            exit 1
          fi

          echo "Struttura Android OK."

      - name: Compila APK Debug
        shell: bash
        run: |
          set -e

          cd "$PROJECT_DIR"

          ./gradlew clean assembleDebug --no-daemon --stacktrace

      - name: Trova APK generato
        shell: bash
        run: |
          set -e

          APK="$(find "$PROJECT_DIR" -type f -name '*.apk' | head -n 1)"

          if [ -z "$APK" ]; then
            echo "ERRORE: nessun APK generato."
            exit 1
          fi

          echo "APK trovato:"
          echo "$APK"

          echo "APK_PATH=$APK" >> "$GITHUB_ENV"

      - name: Carica APK
        uses: actions/upload-artifact@v4
        with:
          name: FrontlineCommander-debug
          path: ${{ env.APK_PATH }}
          if-no-files-found: error
